/**
 * Media library and gallery configuration.
 *
 * Adjust these values when scaffolding a new client site:
 * - maxUploads: total images the library will accept
 * - gallery.maxImages: max images the gallery section can display
 */
export const MEDIA_CONFIG = {
  maxUploads: 10,
  gallery: {
    maxImages: 6,
  },
};
